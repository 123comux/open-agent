fix: deep audit pass — concurrency, resource leaks, sandbox bypass

Critical bug fixes:
- server/api.py: validate kb_name in /api/upload (path traversal);
  close MCP client and model.aclose() in lifespan and update_settings;
  atomic rebuild under asyncio.Lock (_rebuild_lock)
- cli.py: collapse chat/ask into single asyncio.run to avoid httpx
  client cross-loop errors (CLI was unusable)
- memory/session_manager.py: rename_session no longer overwrites the
  renamed disk file with an empty in-memory session (history loss);
  pre-check target existence before mutating state
- tools/mcp/client.py: connect() cleans up on partial failure;
  stdio JSON-RPC serialized via _stdio_lock (concurrent calls no
  longer read each other's responses); close() is exception-safe
- rag/stores/faiss_store.py: query() holds _lock around search+read
  to prevent result misalignment with concurrent delete()
- tools/builtin/browser.py: _with_page uses AsyncExitStack so any
  close() failure cannot leak browser/playwright; _screenshot calls
  check_path; _fetch and all url-accepting actions enforce SSRF
  guard (block private/loopback/link-local IPs incl. 169.254.169.254)

High-impact fixes:
- observability/tracer.py: LangfuseTracer pops _observations after
  end (was unbounded memory growth); except blocks now log.debug
  instead of silently swallowing backend errors
- tools/builtin/python.py: drop getattr/setattr/delattr/super from
  safe_builtins; multiline continuation falls back to exec(code)
  when exec(prefix) hits SyntaxError
- tools/builtin/web_search.py: rate limiter serialized via asyncio.Lock
  (TOCTOU bypass fixed); DuckDuckGo _parse_lite/_parse_html extract
  snippet within each result block instead of global zip (misalignment)
- tools/builtin/file.py: atomic write via tmp + os.replace; missing
  content on write returns error instead of silently truncating file
- rag/kb_manager.py: get-or-create KB under asyncio.Lock (concurrent
  index_file no longer orphans the first KB's documents)
- rag/hybrid_retriever.py + kb_router.py: asyncio.gather with
  return_exceptions=True so one failing leg/KB doesn't cancel others
- rag/reranker.py: lazy model loading (heavy CrossEncoder no longer
  blocks event loop in async __init__)
- agent/core.py: Agent.run/run_stream guarded by _run_lock (shared
  memory no longer races on concurrent calls); exhausted-steps final
  LLM call now wrapped in trace span for observability
- agent/context_window.py: token estimation cached per message
  (eliminates O(n^2) re-encoding); system message tail-truncated
  when it alone exceeds max_tokens
- models/{openai,anthropic,ollama}_provider.py: stream retry only
  when nothing has been yielded yet (prevents duplicate token replay)
- server/api.py: SettingsUpdateRequest numeric fields bounded via
  Field(ge/le); error responses no longer echo str(exc) (info leak);
  get_settings_endpoint reuses Settings.to_safe_dict()

Tests: +51 cases covering rename preservation, FAISS query/delete
concurrency, KB concurrent create, gather return_exceptions, browser
SSRF/screenshot sandbox, python continuation fallback, web_search
rate-limit serialization, atomic file write, context_window caching/
system truncation, provider stream retry semantics, agent run lock.
Full suite 453 passed; ruff and mypy strict clean.
