"""FastAPI server package exposing the agent over HTTP and WebSocket.

Importing this package is lightweight; the FastAPI app is constructed in
:mod:`open_agent.server.api`, which requires the optional ``server`` extra.
"""
from __future__ import annotations
